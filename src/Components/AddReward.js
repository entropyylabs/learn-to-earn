import React from "react";
import { Button } from "antd";

function AddReward() {
  return (
    <div>
      <Button type="primary"> Preview Certificate</Button>
    </div>
  );
}

export default AddReward;
